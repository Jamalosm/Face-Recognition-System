import cv2

def test(image, model_dir, device_id):
    print(f"Testing anti-spoofing with model directory: {model_dir}")
    print(f"Device ID: {device_id}")

    # Placeholder for anti-spoofing detection logic
    # Replace the next line with actual model loading and prediction logic
    label = 1  # Assuming 1 means real face detected

    # Add more logic to load and apply your anti-spoofing model here

    print(f"Test result: {label}")
    return label
